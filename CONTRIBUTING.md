# مشارکت در IVA | Contributing

از مشارکت شما استقبال می‌کنیم. این ریپو **HTML/CSS/JS خالص** است؛ بدون فریم‌ورک، بیلد استپ یا وابستگی خارجی.

## روند پیشنهادی | Suggested flow

1. ریپو را **Fork** کنید.
2. یک Branch با نام روشن بسازید (مثل `feature/recurring-transactions` یا `fix/mobile-overflow`).
3. تغییرات را **کوچک، خوانا و متمرکز** نگه دارید.
4. تست‌های پایهٔ زیر را اجرا کنید.
5. **Pull Request** ایجاد و تغییرات را دقیق توضیح دهید.

## ساختار و معماری | Architecture

```
index.html → js/i18n.js → js/utils.js → js/store.js
           → js/charts.js → js/tools.js → js/jdate.js → js/app.js
```

- `i18n.js`: دیکشنری fa/en و متادیتای دسته‌ها/حساب‌ها
- `utils.js`: Escape، فرمت پول، تاریخ شمسی، آیکون SVG
- `store.js`: وضعیت، اعتبارسنجی، مهاجرت v1/v2→v3، دمو
- `charts.js`: موتور نمودار SVG
- `tools.js`: جعبه‌ابزار مالی (اختیاری، شبکه فقط در این ماژول)
- `jdate.js`: تقویم/انتخابگر شمسی
- `app.js`: روتر، صفحات، فرم‌ها، اکشن‌ها

## استاندارد کد | Code standards

- از JavaScript خالص و APIهای استاندارد مرورگر استفاده کنید.
- **وابستگی خارجی اضافه نکنید** مگر با توافق قبلی.
- همهٔ ورودی‌های کاربر باید از `U.esc()` عبور کنند.
- تغییرات داده باید با `Store.sanitize()` / مهاجرت سازگار باشند.
- هویت بصری و تم روشن/تاریک را حفظ کنید.
- هر دو زبان fa/en و حالت RTL/LTR را در نظر بگیرید.
- اطلاعات حساس/واقعی کاربر را **Commit نکنید**؛ از دادهٔ دمو استفاده کنید.

## تست‌ها | Checks

این پروژه تست خودکار اختصاصی ندارد، اما قبل از PR این‌ها را اجرا کنید:

```bash
# 1) صحت سینتکس همهٔ ماژول‌ها
for f in js/*.js; do node --check "$f"; done

# 2) اجرای محلی
python3 -m http.server 8080
# یا
npx serve .
```

در مرورگر بررسی کنید:

- حالت روشن/تاریک/سیستمی
- فارسی/انگلیسی (RTL/LTR)
- عرض ۳۹۰px و کمتر بدون اورفلو افقی
- دسکتاپ ۱۲۰۰px+ و موبایل
- ثبت/ویرایش/حذف + Undo
- Import/Export JSON و CSV
- دمو از تنظیمات
- صفحهٔ ابزارها (در صورت اتصال)

## گزارش باگ | Bugs

نسخهٔ مرورگر، مراحل بازتولید، نتیجهٔ مورد انتظار و اسکرین‌شات/کنسول خطا را ارائه کنید. از قالب `ISSUE_TEMPLATE` استفاده کنید.

## گزارش آسیب‌پذیری | Vulnerabilities

آسیب‌پذیری را **در Issue عمومی نگذارید**؛ از [SECURITY.md](SECURITY.md) پیروی کنید.
