# سیاست امنیتی | Security Policy

## نسخه‌های پشتیبانی‌شده | Supported versions

| نسخه | وضعیت |
|---|---|
| v2.0.0 (وب) | ✅ پشتیبانی‌شده |
| v1.0.2 (اپ‌های نصبی) | ✅ پشتیبانی‌شده |
| نسخه‌های قدیمی‌تر | ❌ توصیه به ارتقا |

## گزارش آسیب‌پذیری | Reporting a vulnerability

آسیب‌پذیری امنیتی را در **Issue عمومی منتشر نکنید**. لطفاً از حالت **Report a vulnerability** در بخش **Security** ریپوی [Kourosh242/iva-personal-finance](https://github.com/Kourosh242/iva-personal-finance/security) استفاده کنید.

اطلاعات مفید:

- نسخهٔ مرورگر / سیستم‌عامل
- آدرس URL یا صفحهٔ مربوط
- مراحل بازتولید دقیق
- تأثیر احتمالی
- پیشنهاد اصلاح

ما معمولاً آسیب‌پذیری‌های گزارش‌شده را در مدت کوتاهی بررسی و در CHANGELOG ذکر می‌کنیم.

## مدل حریم خصوصی | Privacy model

- داده‌های مالی فقط در **LocalStorage مرورگر / حافظهٔ خصوصی اپ** ذخیره می‌شوند.
- هیچ دادهٔ شخصی یا تراکنشی به هیچ سروری ارسال نمی‌شود.
- بخش «ابزارها» فقط هنگام کلیک کاربر، درخواست **عمومی** به دامنه‌های محدود می‌فرستد (tgju، نو‌بیتکس، ارزدیجیتال، CoinGecko، ...) و هیچ‌چیز جز URL عمومی ندارد.
- سرویس ورکر فقط منابع خودِ اپ را کش می‌کند.

## شیوه‌های امنیتی در کد | Security practices in code

- **CSP**: `index.html` دارای `Content-Security-Policy` محدودکننده است.
- **XSS**: همهٔ ورودی‌ها با `U.esc()` پیش از درج HTML escape می‌شوند.
- **اعتبارسنجی**: `Store.sanitize()` ورودی‌های Import/پشتیبان را پاک‌سازی می‌کند.
- **مهاجرت**: داده‌های v1/v2 بدون حذف بی‌صدا به v3 منتقل می‌شوند.
- **Undo**: عملیات مخرب قابل بازگشت‌اند.
- **لینک‌های خروجی**: `rel="noopener noreferrer"`.

## توصیه‌ها برای کاربران | User recommendations

- پشتیبان JSON را مرتب از **تنظیمات ← داده‌ها** بگیرید.
- فایل‌های نصبی را فقط از **صفحهٔ رسمی ریلیز** دانلود کنید و چک‌سام را بررسی کنید.
- در حالت Private/Incognito، داده‌ها بعد از بستن مرورگر پاک می‌شوند.
- در اندروید حذف اپ = حذف داده؛ قبل از حذف، پشتیبان JSON بگیرید.
