# تغییرات (Changelog)

تمام تغییرات مهم پروژه در این فایل ثبت می‌شوند.

> 🏷️ **دو خط نسخه‌سازی:** پلتفرم وب (این ریپو) با خط نسخهٔ **2.x** و اپ‌های نصبی (فایل‌های آمادهٔ ریلیز) با خط نسخهٔ **1.0.x** شماره‌گذاری می‌شوند. هر دو به‌صورت موازی به‌روز می‌شوند و لازم نیست عددشان یکی باشد.

## 1.0.2 — 2026-08-28 · اپ‌های نصبی

نخستین ریلیز رسمی بسته‌های نصبی آماده (قابل دانلود از [صفحهٔ ریلیز](https://github.com/Kourosh242/iva-personal-finance/releases/tag/1.0.2)):

### 📦 فایل‌های منتشرشده
- 🪟 ویندوز: `IVA-Setup-1.0.2-x64.exe` (۶۴ بیتی) و `IVA-Setup-1.0.2-ia32.exe` (۳۲ و ۶۴ بیتی)
- 🐧 لینوکس: `IVA-1.0.2-amd64.deb` (x86_64) و `IVA-1.0.2-arm64.deb` (ARM64 / رزبری‌پای)
- 📱 اندروید: `IVA-1.0.2-android-universal.apk` — همهٔ گوشی و تبلت‌ها، اندروید ۱۰ به بالا، بدون هیچ مجوزی

### 📖 مستندات ریلیز
- راهنمای کامل انتخاب و نصب فایل مناسب: [DOWNLOADS.md](DOWNLOADS.md) (منبع متن ریلیز)
- بخش «دانلود و نصب» در [README](README.md) و صفحهٔ [دانلود و نصب](wiki/Downloads.md) در ویکی
- امضای SHA256/MD5 همهٔ فایل‌ها برای بررسی سلامت دانلود

---

## 2.0.0 — 2026-08-27 · پلتفرم وب

نسخهٔ ۲ — بازنویسی کامل به‌عنوان یک اپ بانکی درجه‌یک. **۶۱ تست خودکار پایان‌به‌پایان پاس شده‌اند.**

### 🩹 پچ نهایی — 2026-08-29
- **مهاجرت v1→v2**: نوع تراکنش‌های قدیمی حالا از فیلد `type` خوانده می‌شود و علامت مبلغ فقط به‌عنوان پشتیبان (قبل‌تر، یک هزینه با مبلغ مثبت می‌توانست به‌اشتباه «درآمد» دیده شود)
- **فرم «افزودن پس‌انداز»**: فوکوس حالا روی فیلد مبلغ است (قبل‌تر روی فیلد پنهان فرم بود)
- **نمایش اعداد و درصدها**: کسرها درست نمایش داده می‌شوند (مثل «۱۲٫۵٪» به‌جای «۱۳٪»)
- **داشبورد**: مرتب‌سازی پایدار «تراکنش‌های اخیر» با کلید یکتا
- **مستندات**: بازسازی کامل README (جذب کامل راهنمای دانلود `DOWNLOADS.md`)، صفحهٔ جدید ویکی «دانلود و نصب»، متن کامل ریلیز 1.0.2 و هماهنگ‌سازی همهٔ صفحات

### رفع باگ (۲.۰ — layout & logic)


- سایدبار دیگر روی محتوا overlay نمی‌شود؛ در جریان flex و sticky است
- کلیک منوی دسکتاپ (`data-action="go"`) کار می‌کند
- فرم هدف از فیلد `target` خوانده می‌شود (قبلاً همیشه «مبلغ نامعتبر»)
- متغیرهای CSS `--brand-2` / `--line-2` در نمودارها و حلقه سلامت
- گیومهٔ اضافه در زیرعنوان گزارش‌ها
- برچسب ماه اسفند در انگلیسی و سررسید بدهی «امروز» برای فردا
- HTML مودال افزودن به هدف و persist امن LocalStorage

### 🐛 رفع باگ (نسبت به 4.0.0)

- **CSV**: فیلدها Quote می‌شوند؛ کاما در عنوان تراکنش دیگر فایل را خراب نمی‌کند (+ CRLF و BOM برای اکسل)
- **ریسپانسیو**: رفع اورفلو افقی ۳۷۶px موبایل در صفحه نمای کلی (باگ `min-width:auto` آیتم‌های Grid)
- **موبایل**: گزارش‌ها و تنظیمات با Bottom Sheet و نوار ناوبری جدید کاملاً در دسترس هستند (قبلاً حذف می‌شدند)
- **دوزبانه**: ترجمه کامل ۱۰۰٪ رابط؛ در حالت انگلیسی هیچ متن فارسی جامانده نیست
- **امنیت**: Escape کامل ورودی‌ها در HTML (رفع XSS بالقوه در عنوان/یادداشت)
- **Service Worker**: استراتژی network-first + stale-while-revalidate به‌جای cache-first صرف؛ آپدیت سریع‌تر نسخه
- **Manifest**: تفکیک آیکون‌های `any` و `maskable` و افزودن ۱۹۲px + Shortcut
- **داده**: اعتبارسنجی و Sanitize کامل داده‌ها؛ خطای JSON دیگر بی‌صدا نمی‌ماند؛ مهاجرت خودکار v1→v2

### ✨ افزوده شد

- ویرایش و حذف همه موجودیت‌ها با دیالوگ تأیید و **برگردان (Undo)** با یک کلیک
- فرم کامل تراکنش: نوع، دسته، حساب، تاریخ و یادداشت + همگام‌سازی خودکار موجودی
- موتور نمودار SVG اختصاصی: میله‌ای گروهی، خطی نرم، دونات، حلقه پیشرفت، اسپارک‌لاین — همه متحرک، همه آفلاین
- تمام آمار و نمودارها از **داده واقعی** محاسبه می‌شوند (قبلاً اعداد ثابت بودند)
- تقویم شمسی واقعی با `Intl.DateTimeFormat` (بدون وابستگی)، برچسب امروز/دیروز، سررسید بدهی‌ها
- گزارش‌های ۳/۶/۱۲ ماهه: روند خالص، top دسته‌ها، وضعیت بودجه، پیش‌بینی پایان ماه
- سلامت مالی با نمره مرکب واقعی و تحلیل‌های هوشمند خودکار
- جستجوی زنده + فیلتر نوع/دسته/حساب/ماه + مرتب‌سازی + صفحه‌بندی
- بازیابی از پشتیبان JSON (Import) با تأیید و Undo
- واحد پول تومان/ریال (نمایش ×۱۰) + انتخاب تم سیستم/روشن/تاریک
- حالت تاریک بازطراحی‌شده، فونت متغیر وزیرمتن، انیمیشن شمارنده اعداد
- ناوبری موبایل: نوار پایین + دکمه شناور ＋ + Bottom Sheet
- دسترس‌پذیری: ARIA، Focus Trap، Esc، `prefers-reduced-motion`
- میان‌برهای صفحه‌کلید: `N` تراکنش جدید، `/` جستجو، `T` تم، `Esc` بستن
- Hash Routing با دکمه Back مرورگر و Deep-link (`#/transactions?new=1`)
- پوشه `.github` (قالب Issue و PR) که README معرفی کرده بود

### ♻️ تغییرات زیر ساخت

- کد به ۵ ماژول تفکیک شد (`i18n` / `utils` / `store` / `charts` / `app`)
- طرح‌واره داده نسخه ۲ با شناسه‌های پایدار، دسته‌های کلیددار و تاریخ ISO
- کلیدهای LocalStorage: `iva-data-v2` و `iva-settings` (داده v1 به‌طور خودکار مهاجرت می‌یابد)

## 4.0.0 — 2026-08-21 · پلتفرم وب (شماره‌گذاری پیشین، تاریخی)

### اضافه شد

- بازنویسی کامل با HTML، CSS و JavaScript خالص
- داشبورد، تراکنش‌ها، حساب‌ها، بودجه‌ها، اهداف، بدهی‌ها و گزارش‌ها
- حالت روز و شب با حفظ انتخاب کاربر
- رابط فارسی و انگلیسی با RTL و LTR
- خروجی CSV و پشتیبان JSON
- ذخیره اطلاعات در LocalStorage
- طراحی واکنش‌گرا و هویت بصری IVA
- پشتیبانی PWA و اجرای آفلاین
- مستندات حرفه‌ای GitHub
